class Solution {
public:
    void reverseString(vector<char>& str) {
        int n = str.size();
        int i=0 , j=n-1;
        while(i<j)
        {
            swap(str[i],str[j]);
            i++;
            j--;
        }
    }
};
